# BugKu Web14 - 文件上传



## 题目信息

- **平台**: [BugKu CTF](https://ctf.bugku.com/)

- **类型**: Web

- **难度**: ⭐⭐⭐

- **金币**: 4

- **分数**: 35



## 题目描述

打开题目后，页面上有一个文件上传入口，提示只能上传图片格式的文件。上传 PHP 文件会被拒绝，需要绕过后端过滤上传 webshell。



## 解题过程



### 1. 打开题目

访问链接，看到一个文件上传页面，提示只能上传图片文件。



### 2. 尝试上传 PHP 文件

直接上传一个 PHP 一句话木马，提示上传失败，说明后端有文件类型检测。



### 3. 分析过滤机制

使用 Burp Suite 抓包分析，发现后端主要检测以下三点：

1. **文件后缀名**：只允许 jpg、gif、png 等图片后缀
2. **Content-Type**：需要为 image 类型
3. **multipart/form-data**：请求头中必须包含 multipart/form-data 字段



### 4. 绕过上传检测

#### 方法一：修改后缀 + 修改 Content-Type + 修改 multipart/form-data 大小写

将文件后缀从 `.php` 改为 `.php4`（或 `.php5`、`.phtml`），同时修改 Content-Type 为 `image/jpeg`。

**关键步骤：将请求头中的 `multipart/form-data` 改为 `Multipart/form-data`（首字母大写）：**

```
Content-Type: Multipart/form-data; boundary=----WebKitFormBoundaryxxxxxx
```

上传成功，服务器返回文件路径。



### 5. 连接 webshell

使用蚁剑（AntSword）连接上传的 webshell：

```
http://xxx.xxx.xxx.xxx:xxxx/upload/xxxxx.php4
```

连接成功后，进入文件管理，浏览目录结构。



### 6. 获取 Flag

在根目录下发现 flag 文件，打开即可得到 flag：

```
flag{xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx}
```



## 知识点总结

- **文件上传漏洞**: 当 Web 应用对用户上传的文件类型、后缀、内容检测不严格时，可能导致恶意文件上传。

- **后缀绕过技巧**:
  - PHP 可执行后缀：`.php`、`.php2`、`.php3`、`.php4`、`.php5`、`.phtml`、`.phps`、`.pht`
  - 大小写绕过：`Php`、`pHp`、`PHP`
  - 特殊后缀：`.php.`、`.php::$DATA`（Windows 特性）

- **Content-Type 绕过**: 修改请求头中的 Content-Type 字段为 `image/jpeg`、`image/png` 等图片类型。

- **multipart/form-data 大小写绕过**: 将 `multipart/form-data` 中任意字母改为大写（如 `Multipart/form-data`）可绕过某些正则匹配检测。

- **工具连接**: 上传成功后使用蚁剑、菜刀、冰蝎等工具连接 webshell，获取服务器权限。



## 为什么 multipart/form-data 改大小写能绕过？

### 原理分析

后端检测 `multipart/form-data` 时，通常使用**正则匹配**或**字符串比较**。

#### 1. 正则匹配不区分大小写的问题

如果后端代码使用类似以下的正则：

```php
if (preg_match('/multipart\/form-data/', $content_type)) {
    // 认为是合法的上传请求
}
```

**注意**：这个正则**没有使用 `i` 修饰符**（不区分大小写），所以 `Multipart/form-data` 无法匹配。

但如果正则写成了：

```php
if (preg_match('/multipart\/form-data/i', $content_type)) {
    // 使用了 i 修饰符，大小写不敏感
}
```

这种情况下大小写就无法绕过了。

#### 2. 字符串比较的问题

如果后端使用**精确字符串比较**：

```php
if ($content_type === 'multipart/form-data') {
    // 严格匹配
}
```

或者使用 `strpos` 查找子串：

```php
if (strpos($content_type, 'multipart/form-data') !== false) {
    // 查找子串
}
```

这种情况下，`Multipart/form-data` 就无法匹配，因为大小写不同。

#### 3. 为什么这种检测方式存在？

- **开发者的疏忽**：很多开发者认为 `multipart/form-data` 是标准写法，不会考虑大小写变化
- **WAF 规则**：部分 WAF 规则只匹配小写的 `multipart/form-data`，没有考虑大小写绕过
- **历史遗留**：某些旧版本的框架或库存在这种漏洞

#### 4. 实际案例

在 BugKu 这道题中，后端检测逻辑可能是：

```php
// 检测是否为 multipart 上传
if (!strpos($content_type, 'multipart/form-data')) {
    die('非法请求');
}

// 检测文件后缀
if (!in_array($ext, ['jpg', 'gif', 'png'])) {
    die('只允许上传图片');
}
```

当把 `multipart/form-data` 改为 `Multipart/form-data` 时：
- 第一个检测**失败**（找不到 `multipart/form-data`）
- 但 PHP 的 `$_FILES` 处理机制**仍然能正常解析**上传的文件
- 因为 PHP 底层解析 HTTP 请求时，对 `Content-Type` 的大小写是**不敏感**的

这就造成了一个**逻辑漏洞**：
- 应用层的检测认为这不是一个上传请求（放行）
- 但 PHP 底层仍然把它当作上传请求处理（执行）

### 总结

| 层面 | 对大小写的敏感度 | 结果 |
|------|---------------|------|
| PHP 底层解析 | **不敏感** | 正常处理上传 |
| 应用层检测代码 | **敏感**（如果写死了） | 绕过检测 |

所以将 `multipart/form-data` 改为 `Multipart/form-data` 可以绕过应用层的检测，但 PHP 底层仍然能正常解析上传的文件，从而实现绕过。



## 参考

- [BugKu CTF 平台](https://ctf.bugku.com/)
- [CTF-Web 文件上传漏洞做题姿势](https://fanygit.github.io/2021/10/22/CTF-Web%E3%80%90%E6%96%87%E4%BB%B6%E4%B8%8A%E4%BC%A0%E3%80%91%E6%BC%8F%E6%B4%9E%E5%81%9A%E9%A2%98%E5%A7%BF%E7%A7%AF%E7%B4%AF/)
- [HTTP Content-Type 大小写问题](https://www.rfc-editor.org/rfc/rfc2045.html)
