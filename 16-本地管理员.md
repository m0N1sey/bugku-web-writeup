# BugKu Web16 - 本地管理员

## 题目信息

- **平台**: BugKu CTF
- **类型**: Web
- **难度**: ⭐⭐
- **金币**: 2
- **分数**: 15

## 题目描述

打开题目是一个管理员登录页面，提示“IP禁止访问，请联系本地管理员登陆，IP已被记录。”需要通过 IP 伪造绕过限制，并结合页面中的隐藏线索获取登录凭证。

## 解题过程

### 1. 信息收集

打开页面，显示一个管理员登录表单，包含用户名和密码输入框，以及提交按钮。

页面下方有一段提示：

```text
IP禁止访问，请联系本地管理员登陆，IP已被记录。
```

查看网页源代码（`F12`），发现一行注释：

```html
<!-- dGVzdDEyMw== -->
```

将这段 Base64 字符串解码：

```bash
echo "dGVzdDEyMw==" | base64 -d
```

得到：

```text
test123
```

推测这可能是**密码**，而用户名可能是 `admin`（管理员系统常见默认账号）。

### 2. 尝试登录

直接提交 `admin` / `test123`，页面仍然提示“IP禁止访问”，说明后端检测了来源 IP，非本地请求被拒绝。

### 3. IP 伪装绕过

使用 Burp Suite 抓取登录请求，在请求头中添加伪造 IP 的字段：

```http
X-Forwarded-For: 127.0.0.1
```

或者

```http
Client-IP: 127.0.0.1
```

通常 `X-Forwarded-For` 即可绕过。

重新发送请求，服务器认为请求来自本地（127.0.0.1），通过 IP 检测。

### 4. 获取 Flag

伪造 IP 后，提交 `admin` / `test123`，页面返回 Flag：

```text
flag{g361c189a67fc66d34c8f98fd25c4ga5}
```

## 知识点总结

- **Base64 编码/解码**：页面源码中的注释可能隐藏关键信息，需要识别并解码。

- **IP 伪造（IP Spoofing）**：  
  当服务器通过 HTTP 头中的 `X-Forwarded-For`、`Client-IP`、`X-Real-IP` 等字段获取客户端 IP 时，攻击者可以手动修改这些字段来绕过 IP 限制。

- **常见绕过字段**：
  - `X-Forwarded-For: 127.0.0.1`
  - `Client-IP: 127.0.0.1`
  - `X-Real-IP: 127.0.0.1`
  - `X-Originating-IP: 127.0.0.1`

## 原理分析

本题后端代码可能类似于：

```php
$ip = $_SERVER['HTTP_X_FORWARDED_FOR'] ?? $_SERVER['REMOTE_ADDR'];
if ($ip != '127.0.0.1') {
    die('IP禁止访问，请联系本地管理员登陆，IP已被记录。');
}
// 检查用户名密码...
```

由于后端信任了 `X-Forwarded-For` 头，我们伪造它即可绕过。

## 参考

- [BugKu CTF 平台](https://ctf.bugku.com/)